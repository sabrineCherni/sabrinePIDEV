@model IEnumerable<MyTimesheet.Domain.Entities.projets>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.projet_From)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.projet_To)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status_projet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.title_projet)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.projet_From)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.projet_To)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status_projet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.title_projet)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Projet_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Projet_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Projet_ID })
        </td>
    </tr>
}

</table>
